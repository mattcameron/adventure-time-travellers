<%= form_for(challenge) do |f| %>
  <% if challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(challenge.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
      <% challenge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>


  <%= f.fields_for :backers, backer do |ff| %>
    <div class="field">
      <%= ff.label :name %>
      <%= ff.text_field :name %>
    </div>

    <div class="field">
      <%= ff.label :email %>
      <%= ff.text_field :email %>
    </div>  
  <% end %>

  <div class="field">
    <%= render 'shared/credit_card' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
